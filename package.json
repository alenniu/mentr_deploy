{
    "name": "mentr-deploy",
    "version": "1.0.0",
    "main": "server/server.js",
    "repository": {},
    "license": "MIT", 
    "engines": {
      "node": "6.9.1"
    },
    "scripts": {
      "build": "(cd client && npm run build)",
      "install": "(cd client && npm install) && (cd server && npm install)",
      "start": "(cd client && PORT=3000 npm run start) && (cd server && node server.js)"
    }
  }